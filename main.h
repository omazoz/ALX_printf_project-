#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <limits.h>
#include <stdarg.h>
#include <stdio.h>
int print_number(int num);
int _strlen(const char *str);
int _printf(const char *format, ...);
int _putstr(char *str);
int _putchar(char c);

#endif